defaults:
        - _self_
        - runner: extract_frame
        - dataset: badminton
hydra:
  run:
    dir: /home/lars/WASB-SBDT/outputs/${hydra.job.name}/${now:%Y-%m-%d_%H-%M-%S}
output_dir:
seed: 1234

